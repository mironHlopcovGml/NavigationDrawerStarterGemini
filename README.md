# FinRepFromDesk - Финансовый менеджер для Android

Это мобильное приложение для Android, предназначенное для учета личных финансов. Оно позволяет импортировать и анализировать транзакции из банковских SMS и PDF-выписок, предоставляя пользователю наглядное представление о его расходах.

## 🚀 Основные возможности

*   **Импорт транзакций**:
    *   Автоматический импорт из входящих **SMS-сообщений** от банков.
    *   Импорт данных из **PDF-выписок**, скачанных из онлайн-банкинга.
*   **Категоризация расходов**: Автоматическое присвоение категорий тратам на основе MCC-кодов (Merchant Category Code).
*   **Хранение данных**: Все данные надежно хранятся в локальной базе данных SQLite на устройстве.
*   **Визуализация**: Отображение данных в виде списков и, в перспективе, графиков.
*   **Экспорт и импорт**: Возможность выгрузки и загрузки всей базы данных для резервного копирования.
*   **Гибкая настройка**: Конфигурация банков и правил парсинга через JSON-файлы.

## 🛠️ Технологический стек

Проект состоит из нескольких частей, использующих разные технологии:

*   **Основное приложение (Xamarin.Android)**:
    *   **Framework**: Xamarin.Android
    *   **Язык**: C#
    *   **UI**: Android XML, Material Components
    *   **База данных**: Entity Framework Core + SQLite
*   **Кроссплатформенный UI (экспериментальный)**:
    *   **Framework**: .NET MAUI, Blazor Hybrid
    *   **UI-компоненты**: MudBlazor (Material Design)
*   **Ядро и бизнес-логика (`EfcToXamarinAndroid.Core`)**:
    *   **Framework**: .NET 8
    *   **Библиотеки**:
        *   `Microsoft.EntityFrameworkCore.Sqlite` - для работы с базой данных.
        *   `Newtonsoft.Json` - для работы с JSON-конфигурациями.
        *   `Tabula` - для извлечения таблиц из PDF-файлов.

## 🏗️ Структура проекта

Решение (`NavigationDrawerStarter.sln`) включает в себя три основных проекта:

1.  **`NavigationDrawerStarter`**: Основной проект приложения на Xamarin.Android. Содержит "классический" нативный интерфейс, сервисы для работы с платформой Android (чтение SMS, доступ к файлам) и является главной точкой входа.

2.  **`EfcToXamarinAndroid.Core`**: Ядро приложения. Это библиотека .NET 8, в которой содержится вся общая бизнес-логика:
    *   Модели данных (`DataItem`, `Sms` и др.).
    *   Контекст базы данных (`DataItemContext`).
    *   Парсеры для SMS и PDF.
    *   Репозитории для работы с данными.
    *   ViewModel (`MainViewModel`) для управления состоянием UI.

3.  **`MauiWithMudBlazorUi`**: Альтернативная, кроссплатформенная версия UI, созданная с помощью .NET MAUI и Blazor. Этот проект использует то же ядро (`EfcToXamarinAndroid.Core`), но предоставляет современный интерфейс на основе веб-технологий.

## ⚙️ Установка и запуск

Для работы с проектом вам понадобится **Visual Studio** с установленной рабочей нагрузкой **".NET Multi-platform App UI development"**.

1.  Склонируйте репозиторий на ваш компьютер.
2.  Откройте файл `NavigationDrawerStarter.sln` в Visual Studio.
3.  **Для запуска Xamarin.Android версии:**
    *   Назначьте проект `NavigationDrawerStarter` в качестве запускаемого (правый клик по проекту -> "Set as Startup Project").
    *   Выберите эмулятор Android или подключенное физическое устройство.
    *   Нажмите "Run" (F5).
4.  **Для запуска .NET MAUI версии:**
    *   Назначьте проект `MauiWithMudBlazorUi` в качестве запускаемого.
    *   Выберите целевую платформу (Android, Windows).
    *   Нажмите "Run" (F5).

## 🔧 Конфигурация

Гибкость приложения обеспечивается за счет внешних JSON-файлов конфигурации, расположенных в `EfcToXamarinAndroid/Configs/`.

*   **`ConfigBank.json`**: Содержит правила для парсинга SMS от разных банков (ключевые слова, регулярные выражения).
*   **`ConfigMcc.json`**: Содержит словарь MCC-кодов и их описаний на русском языке для категоризации операций.

Для добавления поддержки нового банка или нового MCC-кода достаточно отредактировать соответствующий JSON-файл.